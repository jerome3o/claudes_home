node_modules/
dist/
.env
.claude-session
.restart-signal

# Docker shared folders
shared/
downloads/

# Runtime data (session history, local MCP config with machine-specific paths)
claude-web/data/sessions.json
claude-web/data/mcp-config.json
